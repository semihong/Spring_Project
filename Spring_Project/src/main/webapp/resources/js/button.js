/**
 * detail. jsp에 아이디 값을 주고 했을때> 
 */
$(document).ready(function(){

	// 수정버튼을 클릭했을때
	$("#modify").click(function(){
		$("#form1").attr("action","modify");
		
		

})
	// 삭제버튼을 클릭했을때
	$("#delete").click(function(){
		alert("확인");
		form1.attr("action","delete");
	})
})

//form 태그 필요

